# q_Is is beta distribution
prior.shape1.q_Is: 16
prior.shape2.q_Is: 500

# beta_pre is lognormal 
prior.mulog.beta_pre: 0.6931472 #log(2)
prior.sdlog.beta_pre: 0.175

# k_A is lognormal (technically can be greater than 1, but not negative)
prior.mulog.k_A: -0.9808293 # log(0.375)
prior.sdlog.k_A: 0.02

# k_Is_noins is lognormal (technically can be greater than 1, but not negative)
prior.mulog.k_Is_noins: -0.9162907 # log(0.4)
prior.sdlog.k_Is_noins: 0.2

# k_Is_ins is lognormal (technically can be greater than 1, but not negative)
prior.mulog.k_Is_ins: -0.5108256 #log(0.6)
prior.sdlog.k_Is_ins: 0.05

# gamma_H is lognormal 
prior.mulog.gamma_H: -2.110213 # log(1/8.25)
prior.sdlog.gamma_H: 0.15
  
# m_H is beta distribution
prior.shape1.m_H: 50
prior.shape2.m_H: 200

# prop_death_ins is beta distribution, q_ins is calculated from this
prior.shape1.prop_death_ins: 50
prior.shape2.prop_death_ins: 60

# m_Is_mult is lognormal
prior.mulog.m_Is_mult: 0.5306283 # log(1.7)
prior.sdlog.m_Is_mult: 0.1

# school_closure_effect is beta distribution
prior.shape1.school_closure_effect: 100
prior.shape2.school_closure_effect: 470

# lockdown_effect is beta distribution
prior.shape1.lockdown_effect: 52
prior.shape2.lockdown_effect: 23
